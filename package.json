{
  "name": "reqline-parser",
  "version": "1.0.0",
  "description": "A simple reqline parser and executor built with Node.js and TypeScript",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/app.js",
    "dev": "ts-node-dev --respawn --transpile-only app.ts"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.4",
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.30"
  }
}
